# Vercel installs from requirements.txt (pyproject.toml is for local dev).
# Keep this list in sync with [project.dependencies] in pyproject.toml.
langgraph>=0.4
langchain>=0.3
langchain-google-vertexai>=2.0
langchain-pinecone>=0.2
langchain-text-splitters>=0.3
pinecone>=6.0
supabase>=2.15
fastapi>=0.115
pydantic-settings>=2.8
python-dotenv>=1.1
httpx>=0.27
